import base from "./baseurl";

export default {
  ServiceItemListEndpoint: base.baseurl + '/admin/getItems',
  ServiceItemAddEndpoint: base.baseurl + '/admin/addItems',
  ServiceItemEditEndpoint: base.baseurl + '/admin/updateItems',
}
